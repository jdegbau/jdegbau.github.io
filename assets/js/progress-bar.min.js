let processScroll = () => { let e = document.documentElement, l = document.body, o = e.scrollTop || l.scrollTop, t = (e.scrollHeight || l.scrollHeight) - window.innerHeight, r = o / t * 100; document.getElementById("progress-bar").style.width = 2 * r + "%" }; document.addEventListener("scroll", processScroll);