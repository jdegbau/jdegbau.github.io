---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Navigation from '../../components/Navigation.astro';
import Search from '../../components/Search.astro';
import BlogCard from '../../components/BlogCard.astro';
import { getAllPosts } from '../../lib/posts';

const posts = getAllPosts();
const sortedPosts = posts;
const seoTitle = "Josh's Blog";
const description = 'Check out interesting insights, tips, and guides related to SEO, data analytics, and automation written by Joshua DeGrasse-Baumann.';
---
<BaseLayout
  seoTitle={seoTitle}
  description={description}
  canonical="/blog/"
  image="/assets/img/default-blog-hero.gif"
  imageAlt="Blog landing page hero image"
  keywords={["blog", "SEO", "analytics", "data", "automation"]}
>
  <nav slot="nav">
    <Navigation>
      <Search />
    </Navigation>
  </nav>
  <section class="section">
    <div class="container">
      <h1 class="title">{seoTitle}</h1>
      <p>{description}</p>
      <div class="row columns is-multiline" id="blog-cards">
        {sortedPosts.map((post) => (
          <BlogCard post={post} />
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
